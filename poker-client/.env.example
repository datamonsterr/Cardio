# Poker client -> game server connection
# Use SERVER_ADDRESS for LAN IP (e.g. 192.168.1.50)
SERVER_ADDRESS=localhost
PORT=8080

# Vite-exposed equivalents for the renderer bundle
# (electron-vite/Vite only exposes variables prefixed with VITE_ to import.meta.env)
VITE_SERVER_ADDRESS=localhost
VITE_SERVER_PORT=8080
